<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lingyfh.ui"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="15"/>
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name="MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".TouchImageViewActivity"/>

        <activity android:name=".SingleFingerActivity"/>

        <activity android:name=".DragResizeRotateActivity"/>

        <activity android:name=".RotateActivity"/>

        <activity android:name=".RotateMoveActivity"/>

        <activity android:name=".AddTextActivity"/>
        
    </application>
</manifest>
